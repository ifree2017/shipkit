# ShipKit OpenClaw Stage Prompt

Use ShipKit on project path: {{project_path}}

Run stage: {{stage}}

## Rules

1. Read `shipkit.yaml`, `STATE.md`, and relevant ShipKit protocol/workflow files first.
2. Treat repository artifacts as source of truth, not chat history.
3. Produce or update stage artifacts under the project directory.
4. Write handoff notes under `handoff/` when the stage completes.
5. Run or request relevant ShipKit gates before recommending stage progression.
6. If requirements are unclear, create clarification or blocker artifacts instead of guessing.

## Goal / Context

{{goal}}
