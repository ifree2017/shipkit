# pm-agent-workflow Integration

Use this integration for brownfield projects where existing code must be reverse-engineered into PRD, gap analysis, and interview questions.

ShipKit should store outputs under `evidence/reverse-prd/` and mark every finding as code-confirmed, inferred, needs-confirmation, or unknown.
