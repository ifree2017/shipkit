# OpenSpec Integration

OpenSpec is ShipKit's spec-as-source-of-truth layer. It turns confirmed requirements into machine-checkable project specifications.

PRD remains human-readable. Spec files are the checkable projection used by tests, release gates, and drift checks.
